# =============================================================================
# Aero-Eval Environment Variables
# Copy this file to .env and fill in your keys:  cp .env.example .env
# Reference in YAML configs with ${VAR_NAME} syntax
# =============================================================================

# --- Inference Providers ---

# OpenAI (required for L3 GEval, RAG scorers, and OpenAI provider)
OPENAI_API_KEY=sk-...

# Anthropic (required for Anthropic provider)
ANTHROPIC_API_KEY=sk-ant-...

# vLLM (local server — no API key needed, just the base URL)
VLLM_BASE_URL=http://localhost:8000/v1

# Ollama (local server — no API key needed, just the base URL)
OLLAMA_BASE_URL=http://localhost:11434

# --- Experiment Tracking ---

# Weights & Biases (required when wandb_project is set in config)
WANDB_API_KEY=

# --- Data Sources ---

# Hugging Face (required for gated datasets)
HF_TOKEN=hf_...
